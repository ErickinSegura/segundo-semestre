#include "lista.h"

int main() {

    lista l;\

    l.insertaIni(5);
    l.insertaIni(10);
    l.insertaIni(15);
    l.insertaIni(20);
    l.insertaIni(25);
    l.insertaIni(30);

    l.recorre();

    return 0;
}
